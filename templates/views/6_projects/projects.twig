{% extends "base.twig" %}
{% block content %}

{% include "/templates/partial/pageHeader.twig" with {title: acf.ph_title} %}

<section class="s1_projects">
  <div class="s1_projects__grid">
    {% for post in posts|slice(0, 12) %}
      <a class="s1_projects__single" href="{{get_permalink(post.ID)}}">
        <img class="s1_projects__single--img" src="{{get_the_post_thumbnail_url(post.ID)}}" alt="">
      </a>
    {% endfor %}
  </div>

  <div class="s1_projects__grid">
    {% for post in posts|slice(12, 24) %}
      <a class="s1_projects__single" href="{{get_permalink(post.ID)}}">
        <img class="s1_projects__single--img" src="{{get_the_post_thumbnail_url(post.ID)}}" alt="">
      </a>
    {% endfor %}
  </div>

  <div class="s1_projects__grid">
    {% for post in posts|slice(24, 36) %}
      <a class="s1_projects__single" href="{{get_permalink(post.ID)}}">
        <img class="s1_projects__single--img" src="{{get_the_post_thumbnail_url(post.ID)}}" alt="">
      </a>
    {% endfor %}
  </div>

  <div class="s1_projects__grid">
    {% for post in posts|slice(36, 48) %}
      <a class="s1_projects__single" href="{{get_permalink(post.ID)}}">
        <img class="s1_projects__single--img" src="{{get_the_post_thumbnail_url(post.ID)}}" alt="">
      </a>
    {% endfor %}
  </div>
</section>

{% include "/templates/partial/contactFormSection.twig" %}
{% endblock %}